name: Zip and Deploy

on:
  push:
    branches:
      - main

jobs:
  zip_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Archive Repository
        run: |
          zip -r ${{ github.workspace }}/archive.zip .
        working-directory: ${{ github.workspace }}

      - name: Upload Artifact
        uses: actions/upload-artifact@v2
        with:
          name: my-artifact
          path: ${{ github.workspace }}/archive.zip

      - name: SSH Server Deploy
        uses: kostya-ten/ssh-server-deploy@v4
        with:
          host: 202.176.1.150
          port: 22
          username: server
          password: sanjeev123
          scp_source: ${{ github.workspace }}/archive.zip
          scp_target: /media/server/data-5121/laye

  unzip_and_upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Download Artifact
        uses: actions/download-artifact@v2
        with:
          name: my-artifact
          path: ${{ github.workspace }}

      - name: Unzip the file
        run: |
          unzip ${{ github.workspace }}/archive.zip
          # You can add additional commands here to manipulate the extracted files if needed
